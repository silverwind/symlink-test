on:
  workflow_call:
    inputs:
      required:
        description: a required input
        required: true
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.required }}
